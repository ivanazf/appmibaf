import {useEffect} from 'react';
import {getProductById} from '../../asynMock'

const ItemDetailContainer = () => {
    const [product, setProduct] = const [state, setstate] = useState(initialState);
    return (
        <div>
            <h1>Detalle</h1>
        </div>
    )
}

export default ItemDetailContainer;